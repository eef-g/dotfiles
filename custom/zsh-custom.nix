# This is where the custom zsh configuration is stored
# Below are the default custom configurations -- change these to change the zsh configuration
{
  shellAliases = {
    # Add your custom shell aliases here
    # Example:
    # "aliasName" = "command";
    nv = nvim;
  };
}